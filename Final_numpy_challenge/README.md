## 📊 Project 4: The Data Trifecta (Randomness, Aggregation, Filtering)
This final project demonstrates the complete workflow of a data analyst 
using NumPy.

### Skills Demonstrated:
- **Random Generation:** Using `default_rng()` to create synthetic 
datasets for testing.
- **Statistical Aggregation:** Calculating global metrics (mean) across 
multi-dimensional arrays.
- **Dynamic Filtering:** Using computed values (like the mean) to create 
boolean masks and extract specific data subsets.
